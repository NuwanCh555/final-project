PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/smart-expense-tracker
JWT_SECRET=super_secret_jwt_token_for_smart_expense_tracker_12345
JWT_EXPIRE=24h
NODE_ENV=development

# SMTP Configuration for Email Alerts (e.g. Password Reset)
# Option A: Using Gmail with App Password (RECOMMENDED)
# To use Gmail, set SMTP_SERVICE=gmail, and set your email and App Password below.
SMTP_SERVICE=gmail
SMTP_USER=nchathuranga533@gmail.com
SMTP_PASS="vbta wird agmu zowb"

# Option B: Custom SMTP Server
# Comment out SMTP_SERVICE above and use the following if not using Gmail.
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password

SMTP_FROM=alerts@smart-expense-tracker.com
